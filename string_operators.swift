#! /usr/bin/env swift

// Performing string operations

// Concatenate two strings using the '+' operator
let greeting = "Good" + "Morning"

// Cast an integer or real as a string or concatentate it with another string
let rating = 3.5
var ratingResult = "The resturanut rating is " + (String(rating))

// String interpolation
ratingResult = "The resturanut rating is \(rating)"
